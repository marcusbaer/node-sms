#!/usr/bin/env node

var argv = require('optimist').argv,
    sms = require(__dirname + '/../lib/sendsms');

if (argv.pin) {
	sms.pin(argv.pin);
}

sms.getsms(function(messages){
    console.log(messages);
});
/*
sms.send({
    to: argv.to,       	  // Recipient Phone Number
    text: argv.message    // Text to send
}, function(err, result) {
    // error message in String and result information in JSON
    if (err) {
        console.log(err);
    }
    console.log(result);
});
*/
